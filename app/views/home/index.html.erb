<nav class="navbar navbar-expand navbar-dark bg-primary"> <a href="#menu-toggle" id="menu-toggle" class="navbar-brand"><span class="navbar-toggler-icon"></span></a> <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarsExample02" aria-controls="navbarsExample02" aria-expanded="false" aria-label="Toggle navigation"> <span class="navbar-toggler-icon"></span> </button>
  <div class="collapse navbar-collapse" id="navbarsExample02">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active"> <a class="nav-link" href=<%= root_path%>>AutoBot Automation <span class="sr-only">(current)</span></a> </li>
    </ul>
    <form class="form-inline my-2 my-md-0"> </form>
  </div>
</nav>

<div class="container">
  <% am_p = @all_list.am_profile_with_ok %>
  <% pm_p = @all_list.pm_profile_with_ok %>
  <div>
    <h2>Automate Task</h2>
    <p style="color:red;font-size: smaller;"><strong>AutoBot Runs Every Day between 0500-0900, and 1200-1600.</strong></p>
    <p style="font-size: smaller; color: green;">Today's Total Submitted: <%= am_p.count + pm_p.count %> </p>
  </div>
  <p style="font-size: smaller;">Today's Profile Submitted Details ( 5:AM To 9:AM ).</p>
  <% if am_p.count > 0 %>
    <% am_p.each do |u| %>
      <ul class="list-group" style="font-size: smaller;">
        <li class="list-group-item d-flex justify-content-between align-items-center">
          <%= u.name %>
          <span class="badge badge-primary badge-pill"><%= u.submit_status.capitalize %></span>
        </li>
      </ul>
      <%end%>
  <% else %>
    <ul class="list-group" style="font-size: smaller;">
      <li class="list-group-item d-flex justify-content-between align-items-center">
        No Submission Yet !
        <span class="badge badge-primary badge-pill">0</span>
      </li>
    </ul>
  <% end %>

  <br><p style="font-size: smaller;">Today's Profile Submitted Details ( 12:PM To 6:PM ).</p>
  <% if pm_p.count > 0 %>
    <% pm_p.each do |u| %>
      <ul class="list-group" style="font-size: smaller;">
        <li class="list-group-item d-flex justify-content-between align-items-center">
          <%= u.name %>
          <span class="badge badge-primary badge-pill"><%= u.submit_status.capitalize %></span>
        </li>
      </ul>
    <%end%>
  <% else %>
    <ul class="list-group" style="font-size: smaller;">
      <li class="list-group-item d-flex justify-content-between align-items-center">
        No Submission Yet !
        <span class="badge badge-primary badge-pill">0</span>
      </li>
    </ul>
  <% end %>
  <%if false %>
  <br><p style="font-size: smaller;">Today's Profile Not Submitted Details.</p>
  <% f_p = @all_list.today_failed_profile %>
  <% if f_p.count > 0 %>
    <% f_p.each do |u| %>
      <ul class="list-group" style="font-size: smaller;">
        <li class="list-group-item d-flex justify-content-between align-items-center">
          <%= u.name %>
          <span class="badge badge-primary badge-pill"><%= u.submit_status.capitalize %></span>
        </li>
      </ul>
    <%end%>
  <% else %>
    <ul class="list-group" style="font-size: smaller;">
      <li class="list-group-item d-flex justify-content-between align-items-center">
        No Submission Yet !
        <span class="badge badge-primary badge-pill">0</span>
      </li>
    </ul>
  <% end %>
  <% end %>
  <br>
  <div class="d-flex justify-content-center">
    <%= success %>
  </div>
  <div class="row">
    <div class="col-md-4 col-lg-3">
      <a class="btn btn-primary btn-block" style="font-size: smaller;" href=<%= new_list_path %>>Add Profile</a>
    </div>
  </div>
  <br>
  <div class="row">
    <div class="col-md-4 col-lg-3">
      <a class="btn btn-primary btn-block" style="font-size: smaller;" href=<%= lists_path %>>Profile List</a>
    </div>
  </div>
  <br>
  <div class="row">
    <div class="col-md-4 col-lg-3">
      <a class="btn btn-primary btn-block" style="font-size: smaller;" href=<%= root_path %>>Refresh Stats</a>
    </div>
  </div>

</div>

<div style="padding-top: 55px;">
  <footer class="page-footer font-small blue fixed-bottom" style="background: #007bff; font-size: smaller;" >

    <!-- Copyright -->
    <div class="footer-copyright text-center py-3" style="color: black;">© 2020 Copyright:
      <a href="mailto:adil3267@gmail.com" style="color: white;"> Contact Us </a>
    </div>
    <!-- Copyright -->

  </footer>
</div>